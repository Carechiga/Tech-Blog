
<div class="comments">
    {{#each this}}
    <section class="comment">
          <div class="text">
            {{comment_text}}
        </div>
        <div class="meta">
           - {{user.name}} on {{format_date date_created}}
        </div>      
    </section>
    {{/each}}
</div>